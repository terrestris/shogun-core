<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns:context="http://www.springframework.org/schema/context"
    xmlns:aop="http://www.springframework.org/schema/aop"
    xsi:schemaLocation="http://www.springframework.org/schema/beans
                        http://www.springframework.org/schema/beans/spring-beans.xsd
                        http://www.springframework.org/schema/context
                        http://www.springframework.org/schema/context/spring-context.xsd
                        http://www.springframework.org/schema/aop
                        http://www.springframework.org/schema/aop/spring-aop.xsd">

    <!-- Enable AspectJ auto proxy -->
    <aop:aspectj-autoproxy />

    <!-- Declare a custom PermissionEvaluator We'll rely on the standard
        AclPermissionEvaluator implementation -->
    <bean class="org.springframework.security.acls.AclPermissionEvaluator"
        id="permissionEvaluator">
        <constructor-arg ref="aclService" />
        <property name="sidRetrievalStrategy" ref="sidRetrievalStrategy" />
    </bean>

    <!-- Use a custom SidRetrievalStrategy, which takes care if the principal
        of the current authentication object is a SHOGun2 user object -->
    <bean
        class="de.terrestris.shogun2.security.acl.Shogun2SidRetrievalStrategyImpl"
        id="sidRetrievalStrategy">
        <constructor-arg ref="roleHierarchy" />
    </bean>

    <!-- See http://goo.gl/o42k1S -->
    <bean id="methodExpressionHandler"
        class="org.springframework.security.access.expression.method.DefaultMethodSecurityExpressionHandler">
        <!-- To use hasPermission() in expressions, configure a PermissionEvaluator -->
        <property name="permissionEvaluator" ref="permissionEvaluator" />
        <property name="roleHierarchy" ref="roleHierarchy" />
    </bean>

    <!-- http://static.springsource.org/spring-security/site/docs/3.0.x/apidocs/org/springframework/security/access/hierarchicalroles/RoleHierarchyImpl.html -->
    <bean id="roleHierarchy"
        class="org.springframework.security.access.hierarchicalroles.RoleHierarchyImpl">
        <property name="hierarchy">
            <value>
                ROLE_ADMIN > ROLE_USER
            </value>
        </property>
    </bean>

    <!-- ACL Management Aspect -->
    <bean id="aclManagementAspect" class="de.terrestris.shogun2.security.aspect.AclManagement">
        <property name="aclHandlerFactory" ref="aclHandlerFactory" />
    </bean>

    <!-- The ACL handler factory that should be used by the AclManagement
        aspect. You can write your own implementation and define it here, if you
        need that for projects specifics. -->
    <bean id="aclHandlerFactory" class="de.terrestris.shogun2.security.acl.handle.impl.Shogun2AclHandlerFactory" />


</beans>
